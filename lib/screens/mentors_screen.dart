import 'package:flutter/material.dart';

class MentorsScreen extends StatelessWidget {
  const MentorsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Mentors");
  }
}
